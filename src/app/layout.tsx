import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'China Victory Day Parade 2025 - 80th Anniversary',
  description: 'Commemorating the 80th anniversary of victory in the Chinese People&apos;s War of Resistance Against Japanese Aggression and World War II',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}